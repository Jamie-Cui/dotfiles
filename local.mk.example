# Copy this file to local.mk for machine-local settings.
# local.mk is ignored by Git; command-line variables still take precedence.

FONT_SIZE := 12

# Linux defaults to Fcitx5. Uncomment this when the host uses IBus Rime.
# RIME_TARGET_linux = $(DEPLOY_HOME)/.config/ibus/rime
