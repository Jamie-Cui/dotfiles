#+startup: showall
#+startup: nohideblocks

* dotfiles

Personal Linux and macOS config deployed with [[https://www.gnu.org/software/stow/][GNU Stow]].

** Contents

- [[#prerequisites][Prerequisites]]
    - [[#macos][macOS]]
    - [[#fedora][Fedora]]
    - [[#deploy][Deploy]]
- [[#cross-platform-apps][Cross-platform apps]]
    - [[#oh-my-zsh][Oh My Zsh]]
    - [[#vim-vim][Vim]]
    - [[#neovim-nvim][Neovim]]
    - [[#emacs-emacs][Emacs]]
    - [[#kitty-kitty][Kitty]]
    - [[#rime-rime][Rime]]
    - [[#proxyctl-bin][proxyctl]]
    - [[#codex-skills-skills][Codex skills]]
- [[#gnulinux-fedora-apps][GNU/Linux (Fedora) apps]]
    - [[#tmux-tmux][tmux]]
    - [[#x11-x11][X11]]
    - [[#i3-i3][i3]]
    - [[#i3blocks-i3blocks][i3blocks]]
    - [[#rofi-rofi][Rofi]]
    - [[#hyprland-hypr][Hyprland]]
    - [[#waybar-waybar][Waybar]]
    - [[#dunst-dunst][Dunst]]
    - [[#flameshot-flameshot][Flameshot]]
    - [[#fcitx5-fcitx5][Fcitx5]]
    - [[#gtk-2-gtk-20][GTK 2]]
    - [[#gtk-3-gtk-30][GTK 3]]
    - [[#gtk-4-gtk-40][GTK 4]]
    - [[#imsettings-imsettings][imsettings]]
    - [[#davmail-davmail][DavMail]]
    - [[#isync-isync][isync]]
- [[#macos-apps][MacOS apps]]
    - [[#aerospace-aerospace][AeroSpace]]

** Prerequisites

*** macOS

#+begin_src bash
xcode-select --install
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install stow
#+end_src

*** Fedora

#+begin_src bash
sudo dnf install git make stow
#+end_src

*** Deploy

#+begin_src bash
mkdir -p ~/opt
git clone https://github.com/Jamie-Cui/dotfiles.git ~/opt/dotfiles
cd ~/opt/dotfiles
make dry-run
make bootstrap
#+end_src

** Cross-platform apps

Run =make bootstrap= once after installing the apps below.

*** Oh My Zsh

#+begin_src bash
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
#+end_src

*** Vim (=vim=)

#+begin_src bash
brew install vim                 # macOS
sudo dnf install vim-enhanced    # Fedora
#+end_src

*** Neovim (=nvim=)

#+begin_src bash
brew install neovim              # macOS
sudo dnf install neovim          # Fedora
#+end_src

*** Emacs (=emacs=)

#+begin_src bash
git clone --branch emacs-31 --single-branch https://github.com/emacs-mirror/emacs.git ~/opt/emacs-src
#+end_src

**** macOS

#+begin_src bash
brew install autoconf automake texinfo libgccjit gcc gnutls tree-sitter@0.25 pkg-config coreutils sqlite librsvg

cd ~/opt/emacs-src
export PATH="$(brew --prefix texinfo)/bin:$PATH"
export PKG_CONFIG_PATH="$(brew --prefix tree-sitter@0.25)/lib/pkgconfig${PKG_CONFIG_PATH:+:$PKG_CONFIG_PATH}"
./autogen.sh
./configure --with-ns --with-native-compilation --with-tree-sitter --with-sqlite3 --with-rsvg --without-pop --without-mailutils
make -j"$(sysctl -n hw.logicalcpu)"
make install
cp -R nextstep/Emacs.app /Applications/
sudo mkdir -p /usr/local/bin
sudo ln -sf /Applications/Emacs.app/Contents/MacOS/Emacs /usr/local/bin/emacs
sudo ln -sf /Applications/Emacs.app/Contents/MacOS/bin/emacsclient /usr/local/bin/emacsclient
#+end_src

**** Fedora

#+begin_src bash
sudo dnf install dnf-plugins-core gcc gcc-c++ make autoconf automake texinfo
sudo dnf builddep emacs
sudo dnf install libgccjit-devel tree-sitter-devel sqlite-devel librsvg2-devel gtk3-devel

cd ~/opt/emacs-src
./autogen.sh
./configure --with-pgtk --with-native-compilation --with-tree-sitter --with-sqlite3 --with-rsvg --without-pop --without-mailutils
make -j"$(nproc)"
sudo make install
#+end_src

**** Configure

#+begin_src bash
emacs --version

cp ~/.emacs.d/local.el.example ~/.emacs.d/local.el
cp ~/.emacs.d/early-local.el.example ~/.emacs.d/early-local.el
#+end_src

Edit only the copied =local.el= files for machine-specific settings.

*** Kitty (=kitty=)

#+begin_src bash
brew install --cask kitty        # macOS
sudo dnf install kitty           # Fedora
#+end_src

*** Rime (=rime=)

#+begin_src bash
brew install --cask squirrel-app           # macOS
sudo dnf install fcitx5 fcitx5-rime        # Fedora
#+end_src

*** proxyctl (=bin=)

#+begin_src bash
proxyctl init
source ~/.zshrc
#+end_src

*** Codex skills (=skills=)

No setup beyond =make bootstrap=.

** GNU/Linux (Fedora) apps

Run =make bootstrap= once after installing the apps needed by the chosen X11 or
Wayland session.

*** tmux (=tmux=)

#+begin_src bash
sudo dnf install tmux
#+end_src

*** X11 (=x11=)

#+begin_src bash
sudo dnf install xorg-x11-server-Xorg xorg-x11-xinit xorg-x11-xauth
#+end_src

*** i3 (=i3=)

#+begin_src bash
sudo dnf install i3 i3lock picom feh xclip dex-autostart network-manager-applet pulseaudio-utils ibus
#+end_src

*** i3blocks (=i3blocks=)

#+begin_src bash
sudo dnf install i3blocks alsa-utils
#+end_src

*** Rofi (=rofi=)

#+begin_src bash
sudo dnf install rofi
#+end_src

*** Hyprland (=hypr=)

#+begin_src bash
sudo dnf install hyprland hyprlock hyprpaper xdg-desktop-portal-hyprland brightnessctl playerctl wireplumber xclip
#+end_src

*** Waybar (=waybar=)

#+begin_src bash
sudo dnf install waybar jq playerctl blueman network-manager-applet NetworkManager-tui pavucontrol htop baobab
#+end_src

*** Dunst (=dunst=)

#+begin_src bash
sudo dnf install dunst
#+end_src

*** Flameshot (=flameshot=)

#+begin_src bash
sudo dnf install flameshot
#+end_src

*** Fcitx5 (=fcitx5=)

#+begin_src bash
sudo dnf install fcitx5 fcitx5-rime fcitx5-gtk fcitx5-qt
#+end_src

*** GTK 2 (=gtk-2.0=)

#+begin_src bash
sudo dnf install gtk2
#+end_src

*** GTK 3 (=gtk-3.0=)

#+begin_src bash
sudo dnf install gtk3
#+end_src

*** GTK 4 (=gtk-4.0=)

#+begin_src bash
sudo dnf install gtk4
#+end_src

*** imsettings (=imsettings=)

#+begin_src bash
sudo dnf install imsettings
#+end_src

*** DavMail (=davmail=)

#+begin_src bash
sudo dnf copr enable mguessan/davmail
sudo dnf install davmail java-latest-openjdk-headless
mkdir -p ~/.local/state/davmail
systemctl --user enable --now davmail.service
#+end_src

*** isync (=isync=)

#+begin_src bash
sudo dnf install isync maildir-utils
# Configure ~/.authinfo before the first sync.
mbsync outlook
#+end_src

** MacOS apps

Run =make bootstrap= after installing the app.

*** AeroSpace (=aerospace=)

#+begin_src bash
brew install --cask nikitabobko/tap/aerospace
brew tap FelixKratz/formulae
brew install borders

# HACK fix mission control
defaults write com.apple.dock expose-group-apps -bool true && killall Dock

# HACK fix dock (additional turn on autohide in settings)
defaults write com.apple.dock pinning -string start && killall Dock
#+end_src
