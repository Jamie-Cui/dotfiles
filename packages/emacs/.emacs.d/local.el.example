;;; local.el.example --- Machine-local Emacs settings -*- lexical-binding: t -*-

;; Copy this file to ~/.emacs.d/local.el.  That file is intentionally not
;; managed by Stow or Git.

;; (setq +emacs/org-root-dir (expand-file-name "~/opt/org-root"))
;; (setq +emacs/proxy "127.0.0.1:10808")
;; (setq +emacs/theme 'zenburn)
;; (setq +emacs/disabled-modules '("llm" "lang/lean"))

;; Mail identity and installation-specific mu4e path.
;; (setq +emacs/email-address "you@outlook.com"
;;       +emacs/email-full-name "Your Name"
;;       +emacs/email-maildir (expand-file-name "~/.local/share/mail/outlook")
;;       +emacs/mu4e-load-path "/usr/share/emacs/site-lisp/mu4e")
;; Apple Silicon Homebrew alternative:
;; (setq +emacs/mu4e-load-path "/opt/homebrew/share/emacs/site-lisp/mu/mu4e")

;; If the official package hosts are slow:
;; (setq package-archives
;;       '(("gnu"    . "https://mirrors.tuna.tsinghua.edu.cn/elpa/gnu/")
;;         ("nongnu" . "https://mirrors.tuna.tsinghua.edu.cn/elpa/nongnu/")
;;         ("melpa"  . "https://mirrors.tuna.tsinghua.edu.cn/elpa/melpa/")))

;; If you use an Apple keyboard, map the Super key to Meta.
;; (setq x-super-keysym 'meta)

;;; local.el.example ends here
