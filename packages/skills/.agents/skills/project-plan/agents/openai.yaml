interface:
  display_name: "Project Plan"
  short_description: "Manage helper-resolved project plans"
  default_prompt: "Use $project-plan to review or update the Emacs-resolved central project plan, marking agent-generated actionable subtrees with :agent:."

policy:
  allow_implicit_invocation: false
